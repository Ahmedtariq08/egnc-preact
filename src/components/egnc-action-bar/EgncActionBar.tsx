import { ActionBarProps } from "../../common/action-bar/ActionBar"

interface EgncActionBarProps extends ActionBarProps {
    name: string
}

/**
 * @usage Implements addremove columns and reorder columns within action bar
 */
export const EgncActionBar = (props: EgncActionBarProps) => {
    const { actions } = props;

}